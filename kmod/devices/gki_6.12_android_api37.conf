# SPDX-License-Identifier: GPL-2.0-or-later
#
# Android API 37 GKI 6.12 (AVD API 37, 16K pages)
# CRCs migrated from kmod/shim/shim.h:207-218 (GKI 6.1 reference defaults;
# re-probed per kernel in Plan 2 Phase 7). Struct module offsets migrated
# from tools/kmod_loader/kmod_loader.c presets[].

[identity]
name              = gki_6_12_android_api37
description       = Android API 37 GKI 6.12 (AVD API 37, 16K pages)
arch              = aarch64

[match]
kernelrelease     = 6.12.

[modversions]
module_layout     = 0xea759d7f
_printk           = 0x92997ed8
memcpy            = 0x4829a47e
memset            = 0xdcb764ad

[struct_module]
this_module_size   = 0x640
module_init_offset = 0x188
module_exit_offset = 0x5f8

[vermagic]
string = "6.12.0 SMP preempt mod_unload modversions aarch64"

[provenance]
verified       = true
source_module  = (migrated from shim.h defaults + kmod_loader.c presets[])
extracted_at   = 2026-04-09
